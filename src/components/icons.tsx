import { LucideProps, User, Bot, BrainCircuit, Upload, Send, Search, ChevronsUpDown, Loader2, Layers } from 'lucide-react';

export const Icons = {
  logo: Layers,
  user: User,
  bot: Bot,
  brain: BrainCircuit,
  upload: Upload,
  send: Send,
  search: Search,
  sort: ChevronsUpDown,
  spinner: Loader2,
};
